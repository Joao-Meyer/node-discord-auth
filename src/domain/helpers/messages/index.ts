import { authMessages, defaultMessages, yupMessages } from './list';
import { chatMessages } from './list/chat';

export const messages = {
  auth: authMessages,
  chat: chatMessages,
  default: defaultMessages,
  yup: yupMessages
};
